package Structure;/*


specs:
    -an profile is created in the c'tor,
        -will assign nextId to its acctId
        -generate a map from strings of semesters to corresponding m_courses index
        
 */
public class MyTransfer {
    
//data fields
    public Profile m_profile;
    public int nextId;
    
    
//member functions
    public MyTransfer(){}
}
